server:
  port: 8085

spring:
  application:
    name: REVIEW-SERVICE

    spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/salon_review_db
    username: postgres
    password: Rani@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"